apiVersion: v1
kind: Service
metadata:
  name: {{ include "gnosari-docs.fullname" . }}
  labels:
    {{- include "gnosari-docs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "gnosari-docs.selectorLabels" . | nindent 4 }}